# 使用说明

## 一、环境准备

确保本地已安装以下服务并运行：

| 服务 | 端口 | 备注 |
|------|------|------|
| MySQL | 3306 | root / 123456 |
| Redis | 6379 | 密码 123456，数据库索引 1 |

Redis 配置在 `GD_shop_backend/src/main/resources/application.yaml`，如需修改密码或端口请同步修改配置。

---

## 二、初始化数据库

1. 打开 MySQL 客户端（Navicat、DBeaver 或命令行）
2. 新建数据库 `shop_sp`（字符集 utf8mb4）
3. 执行项目根目录下的 `db.sql`

```sql
-- 在 MySQL 命令行中：
CREATE DATABASE IF NOT EXISTS shop_sp DEFAULT CHARSET utf8mb4;
USE shop_sp;
SOURCE e:/WEB/someworks/shop/db.sql;
```

执行完成后会创建 14 张表并插入种子数据。

---

## 三、启动服务

启动顺序：**数据库 → 后端 → 前端**

### 3.1 启动后端

```bash
cd GD_shop_backend

# 开发模式
mvn spring-boot:run

# 或打包运行
mvn clean package -DskipTests
java -jar target/GD_shop_backend-0.0.1-SNAPSHOT.jar
```

后端运行在 **http://localhost:8082**。

### 3.2 启动管理端（商家后台）

```bash
cd GD_shop
npm install        # 首次运行
npm run dev
```

管理端运行在 **http://localhost:3001**。

### 3.3 启动用户端（商城前台）

```bash
cd GD_shop_frontend
npm install        # 首次运行
npm run dev
```

用户端运行在 **http://localhost:3000**。

---

## 四、默认账号

| 角色 | 手机号 | 密码 | 说明 |
|------|--------|------|------|
| 超级管理员 | 13600000000 | 123456 | 拥有管理端全部权限 |
| 运营 | 13600000003 | 123456 | 可管理商品/订单/秒杀，不可管理用户 |
| 普通用户 | 13600000001 | 123456 | 只能访问用户端 |

共计 10 个种子用户（手机号 13600000000 至 13600000009）。

---

## 五、角色权限体系

| 权限 | 超级管理员(1) | 运营(2) | 普通用户(0) |
|------|:---:|:---:|:---:|
| 登录管理端 | 是 | 是 | 否 |
| 登录用户端 | 是 | 是 | 是 |
| 管理商品（SPU/SKU/分类） | 是 | 是 | — |
| 管理订单（发货/退款） | 是 | 是 | — |
| 秒杀活动管理 | 是 | 是 | — |
| 查看数据统计 | 是 | 是 | — |
| 系统配置 | 是 | 是 | — |
| 用户管理（角色/删除） | 是 | **否** | — |
| 查看操作日志 | 是 | 是 | — |

运营看不到侧边栏的"用户管理"菜单，且对应的接口会被拦截返回 403。

---

## 六、功能详解

### 6.1 用户端（端口 3000）

#### 登录
- 支持"手机号 + 验证码"和"密码登录"两种方式
- 验证码会打印在后端控制台日志中（搜索 `验证码:`）
- 登录后 token 保存在 localStorage

#### 商品浏览
- 首页展示商品列表，支持分类筛选
- 点击商品进入详情页查看多规格 SKU

#### 购物车
- 未登录也可操作，基于浏览器 sessionId 存储在 Redis 中
- 支持加减数量、删除商品
- 结算前需填写收货信息（收货人、电话、地址）
- 已登录用户自动回填默认收货信息

#### 下单
- 点击"结算"按钮提交订单
- 订单包含收货信息、商品明细、总金额
- 下单后可在"我的订单"查看

#### 订单
- 查看订单列表及详情
- 支持按状态筛选

#### 秒杀
- 在秒杀页面查看正在进行或即将开始的秒杀活动
- 活动开始后抢购按钮点亮
- 一人一单，通过 Redis + Lua 脚本保证原子性

#### 个人中心
- 修改收货信息
- 修改登录密码
- 每日签到（连续签到天数用 Redis BitMap 记录）

---

### 6.2 管理端（端口 3001）

#### 登录
- 仅角色 1（超级管理员）和角色 2（运营）可登录管理端
- 登录后左侧菜单根据角色自动展示

#### 工作台
- 欢迎页，展示最近 7 日销售柱状图（订单数 + 销售额）

#### 商品管理
- **商品列表** — 查看、搜索、新增、编辑、删除商品（SPU）
- **分类管理** — 管理商品分类

#### 订单管理
- 查看所有订单，支持按状态筛选
- 操作：发货、退款

#### 秒杀管理
- 创建秒杀活动（选择已有 SPU/SKU，设置秒杀价和库存）
- 查看所有秒杀活动
- 删除活动

#### 用户管理（仅超级管理员可见）
- 查看注册用户列表，支持昵称/手机号搜索
- 切换角色：超级管理员 ↔ 运营 ↔ 普通用户
- 删除用户（不可删除 ID=1 的超管）

#### 系统配置
- 店铺名称、Logo、客服电话、公告
- 支付开关（微信支付 / 支付宝），默认均为开启（值为 1）

#### 操作日志
- 查看管理后台操作记录，支持关键词、类型、日期范围筛选

---

### 6.3 后端调用规则

#### 公开接口（无需登录）
| 路径 | 说明 |
|------|------|
| `POST /user/code` | 发送验证码 |
| `POST /user/login` | 登录 |
| `GET /category/list` | 分类列表 |
| `GET /spu/list` | 商品列表 |
| `GET /spu/{id}` | 商品详情 |
| `GET /sku/list/{spuId}` | SKU 列表 |
| `GET /seckill/list` | 秒杀列表 |
| `/cart/**` | 购物车操作 |
| `GET /config` | 店铺配置 |

#### 需登录接口
其余所有接口均需携带 token（请求头 `Authorization` 或 `token`）。

#### 管理端独占接口
`/spu`、`/sku`、`/category` 的写操作，订单管理、秒杀管理、统计、日志、系统配置等。

---

## 七、Redis 数据结构

| Key | 类型 | 说明 |
|-----|------|------|
| `login:code:{phone}` | String | 验证码，2 分钟过期 |
| `login:token:{token}` | Hash | 用户登录态，10 小时过期 |
| `cache:spu:{id}` | String | 商品缓存，30 分钟过期 |
| `seckill:stock:{activityId}` | String | 秒杀库存 |
| `seckill:order:{activityId}` | Set | 秒杀成功用户集合 |
| `seckill:order:queue` | List | 秒杀订单异步处理队列 |
| `cart:{sessionId}` | Hash | 用户购物车 |
| `sign:{userId}:{yyyyMM}` | BitMap | 签到记录 |

---

## 八、生产构建

### 后端
```bash
cd GD_shop_backend
mvn clean package -DskipTests
# jar 包位于 target/GD_shop_backend-0.0.1-SNAPSHOT.jar
```

### 管理端
```bash
cd GD_shop
npm run build
# 输出到 dist/，部署到任意静态服务器
```

### 用户端
```bash
cd GD_shop_frontend
npm run build
# 输出到 dist/，部署到任意静态服务器
```

---

## 九、常见问题

**Q: 后端启动报 `SerializedLambda` 错误？**
A: 需要在 JVM 启动参数加上：
```
--add-opens java.base/java.lang.invoke=ALL-UNNAMED
--add-opens java.base/java.lang.reflect=ALL-UNNAMED
--add-opens java.base/java.lang=ALL-UNNAMED
--add-opens java.base/java.util=ALL-UNNAMED
```

**Q: 端口被占用？**
A: 检查 8082（后端）、3000（用户端）、3001（管理端）是否已被占用，先关掉再启动。

**Q: 验证码在哪里？**
A: 验证码会打印在后端控制台日志中，搜索 `验证码` 即可找到。

**Q: 如何添加更多默认商品？**
A: 在  的种子数据 `INSERT INTO tb_spu` 部分添加，或通过管理端界面新增。
